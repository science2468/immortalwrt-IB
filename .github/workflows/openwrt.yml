name: Build xg-140g-md
on: 
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      TZ: Asia/Shanghai
    steps:
      - name: Checkout Projects
        uses: actions/checkout@v2

      - name: Clean up the system
        run: |
          sudo rm -rf /usr/share/dotnet /etc/mysql /etc/php /usr/local/lib/android
          sudo -E apt-get -y purge azure-cli ghc* zulu* hhvm llvm* firefox google* dotnet* powershell openjdk* adoptopenjdk* mysql* php* mongodb* dotnet* moby* snapd* || true
          sudo -E apt-get -y autoremove --purge
          sudo -E apt-get clean

      - name: Installation depends
        run: |
          sudo apt update -y
          sudo apt install -y build-essential clang flex bison g++ gawk \
          gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
          python3-setuptools rsync swig unzip zlib1g-dev file wget

      - name: Clone source code
        run: |
          git clone -b openwrt-25.12 https://github.com/openwrt/openwrt.git

          cd openwrt/ && ./scripts/getver.sh
          
      - name: Cache OpenWrt build
        uses: actions/cache@v4
        with:
        path: |
          openwrt/dl
          openwrt/build_dir
          openwrt/staging_dir
        key: openwrt-${{ runner.os }}-${{ hashFiles('openwrt/feeds.conf.default', 'openwrt/.config') }}
        restore-keys: |
          openwrt-${{ runner.os }}-

      - name: Download
        run: |
          ls -a $GITHUB_WORKSPACE

          mv -f xg-140g-md/01_leds openwrt/target/linux/airoha/an7581/base-files/etc/board.d/
          mv -f xg-140g-md/02_network openwrt/target/linux/airoha/an7581/base-files/etc/board.d/
          mv -f xg-140g-md/403-mtd-spinand-add-support-for-FudanMicr-FM25G02B.patch openwrt/target/linux/generic/pending-6.12/
          mv -f xg-140g-md/901-snand-mtk-bmt-support.patch openwrt/target/linux/airoha/patches-6.12/
          mv -f xg-140g-md/an7581-nokia_xg-140g-md.dts openwrt/target/linux/airoha/dts/
          mv -f xg-140g-md/an7581.mk openwrt/target/linux/airoha/image/

          rm -rf openwrt/target/linux/airoha/patches-6.12/910-0*

          cd openwrt/

          ./scripts/feeds update -a && ./scripts/feeds install -a

          cat > .config << EOF
          CONFIG_TARGET_airoha=y
          CONFIG_TARGET_airoha_an7581=y
          CONFIG_TARGET_airoha_an7581_DEVICE_nokia_xg-140g-md=y
          EOF

          make defconfig && make -j $(nproc) download

      - name: target/linux prepare compile
        run: |
          cd openwrt/
          make -j $(nproc) target/linux/{clean,prepare} V=s

      - name: Build
        run: |
          cd openwrt/
          ./scripts/getver.sh

          make -j $(nproc) V=s
  
      - name: Set Date and Time
        id: datetime
        run: echo "::set-output name=datetime::$(date +'%Y%m%d%H%M%S')"

      - name: Upload openwrt firmware
        uses: softprops/action-gh-release@master
        with:
          tag_name: openwrt-xg-140g-md-${{ steps.datetime.outputs.datetime }}
          files: openwrt/bin/targets/airoha/an7581/*
          
