name: sdk
on: 
  workflow_dispatch:
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Projects
      uses: actions/checkout@v2
      
    - name: Installation depends
      run: |
        sudo -E apt update -y
        sudo -E apt install -y ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
        bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib \
        git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libglib2.0-dev libgmp3-dev libltdl-dev \
        libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libreadline-dev libssl-dev libtool lrzsz \
        mkisofs msmtp nano ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools \
        libpython3-dev qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip \
        vim wget xmlto xxd zlib1g-dev python3-setuptools tree
        
    - name: Clone source code
      run: |
        wget https://downloads.openwrt.org/releases/25.12.2/targets/ramips/mt7621/openwrt-sdk-25.12.2-ramips-mt7621_gcc-14.3.0_musl.Linux-x86_64.tar.zst
        tar -I zstd -xvf openwrt-sdk-*.zst && rm -r openwrt-sdk-*.zst

    - name: add custom package
      run: |
        cd openwrt-sdk*
        git clone --depth=1 https://github.com/lmq8267/tunsafe.git
        ls
        mv openwrt tunsafe
        cp -r tunsafe package/
    - name: update feed
      run: |
        cd openwrt-sdk*
        ./scripts/feeds update -a
        ./scripts/feeds install -a

        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: download
      run: |
        cd openwrt-sdk*
        make download -j$(nproc)
        
    - name: build
      run: |
        cd openwrt-sdk*
        make -j$(nproc) package/tunsafe/compile V=99
        
    - name: Upload openwrt firmware
      uses: actions/upload-artifact@master
      with:
        name: openwrt-sdk
        path: openwrt-sdk*/bin/**/*tunsafe*
